{
  "name": "axs-scorecalculator",
  "productName": "AxSCalculator",
  "version": "2.1.7",
  "description": "A calculator to calculate the score with the AxS score format",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . --overwrite --asar --platform=win32 --arch=ia32 --icon=assets/images/axs.ico --prune=true --out=release-builds",
    "package-linux": "electron-packager . --overwrite --asar=true --platform=linux --arch=x64 --icon=assets/images/axs.png --prune=true --out=release-builds"
  },
  "repository": "",
  "keywords": [],
  "author": "Wesley",
  "license": "ISC",
  "devDependencies": {
    "electron": "^3.0.0",
    "electron-packager": "^13.0.1"
  },
  "dependencies": {
    "@octokit/rest": "^16.15.0",
    "electron-settings": "^3.2.0",
    "electron-store": "^2.0.0",
    "glob": "^7.1.3",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  }
}
